{
  "name": "deploy",
  "version": "1.0.0",
  "description": "Deploy build to GitHub Pages",
  "main": "index.js",
  "scripts": {
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
